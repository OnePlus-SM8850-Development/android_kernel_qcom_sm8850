################################################################################
## Inheriting configs from ACK
. "${ROOT_DIR}/vendor/qcom/kernel/build.config.common"
. "${ROOT_DIR}/vendor/qcom/kernel/build.config.aarch64"

################################################################################
## Variant setup
MSM_ARCH=bengal_le
VARIANTS=(defconfig debug-defconfig)
[ -z "${VARIANT}" ] && VARIANT=debug-defconfig

ABL_SRC=bootable/bootloader/edk2
BOOT_IMAGE_HEADER_VERSION=2
BASE_ADDRESS=0x80000000
PAGE_SIZE=4096
BUILD_BOOT_IMG=1
DTB_IMG_CREATE=1
PREFERRED_USERSPACE=le
SKIP_UNPACKING_RAMDISK=1
[ -z "${DT_OVERLAY_SUPPORT}" ] && DT_OVERLAY_SUPPORT=0

if [ "${KERNEL_CMDLINE_CONSOLE_AUTO}" != "0" ]; then
	KERNEL_CMDLINE+='loglevel=8'
fi
